import { IsPermitDirective } from './is-permit.directive';

describe('IsPermitDirective', () => {
  it('should create an instance', () => {
    const directive = new IsPermitDirective();
    expect(directive).toBeTruthy();
  });
});
